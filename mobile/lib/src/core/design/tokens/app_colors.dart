import 'package:flutter/material.dart';

/// Semantic color tokens for CORRIENTAZO.
class AppColors {
  // Brand
  static const brand = Color(0xFF0F766E);
  static const brandDeep = Color(0xFF0B5A54);
  static const accent = Color(0xFFF97316); // warm accent (use sparingly)

  // Neutrals
  static const bg = Color(0xFFF6F7FB);
  static const surface = Color(0xFFFFFFFF);
  static const surface2 = Color(0xFFF9FAFB);
  static const border = Color(0xFFE6EAF2);

  static const text = Color(0xFF0F172A);
  static const text2 = Color(0xFF64748B);

  // Semantic states
  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);
  static const info = Color(0xFF2563EB);

  // Dark
  static const bgDark = Color(0xFF0B1220);
  static const surfaceDark = Color(0xFF0F172A);
  static const borderDark = Color(0xFF1F2A44);
  static const textDark = Color(0xFFF8FAFC);
  static const text2Dark = Color(0xFF94A3B8);
}

