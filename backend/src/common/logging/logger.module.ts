/**
 * Logger module (stub).
 *
 * En producción:
 * - integrar pino/winston,
 * - enrichers con correlationId y request metadata,
 * - logs JSON consistentes para observabilidad.
 */
export class LoggerModule {}

